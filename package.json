{
  "name": "marketing-command-center",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter social db:generate",
    "db:push": "pnpm --filter social db:push",
    "db:migrate": "pnpm --filter social db:migrate",
    "db:studio": "pnpm --filter social db:studio",
    "db:seed": "pnpm --filter social db:seed"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@9.15.0"
}

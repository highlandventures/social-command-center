---
phase: 07-scheduling-ad-hoc-reports
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/lib/scheduling/schedule-manager.test.js
  - __tests__/lib/adhoc-report/adhoc-chat.test.js
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, DIST-01, DIST-03, ADHC-01, ADHC-02, ADHC-03, ADHC-04, ADHC-05]

must_haves:
  truths:
    - "Test stubs exist for all Phase 7 requirements before implementation begins"
  artifacts:
    - path: "__tests__/lib/scheduling/schedule-manager.test.js"
      provides: "Test stubs for SCHED-01..04, DIST-01, DIST-03"
      contains: "describe.skip"
    - path: "__tests__/lib/adhoc-report/adhoc-chat.test.js"
      provides: "Test stubs for ADHC-01..05"
      contains: "describe.skip"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for all Phase 7 modules before implementation begins.

Purpose: Establish test contracts upfront so implementation plans have automated verification from the start. Follows project convention of `describe.skip` stubs with dynamic imports.
Output: Two test files covering scheduling (SCHED + DIST) and ad hoc reports (ADHC) requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scheduling-ad-hoc-reports/07-RESEARCH.md
@.planning/phases/07-scheduling-ad-hoc-reports/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for scheduling, cron, and ad hoc modules</name>
  <files>__tests__/lib/scheduling/schedule-manager.test.js, __tests__/lib/adhoc-report/adhoc-chat.test.js</files>
  <action>
Create two test stub files following project convention (describe.skip with dynamic imports, per Phase 04/06 pattern).

**File 1: `__tests__/lib/scheduling/schedule-manager.test.js`**
- `describe.skip('schedules.create')` — SCHED-01: creates schedule with cadence enum (WEEKLY/MONTHLY/QUARTERLY/YEARLY), validates required fields (name, cadence, reportType, recipients)
- `describe.skip('schedules.list')` — SCHED-04: returns all schedules with nextRunAt, lastRunAt, lastReportId fields
- `describe.skip('schedules.toggle')` — SCHED-03: toggles enabled flag, updates updatedAt
- `describe.skip('schedules.update')` — SCHED-03: edits schedule name, cadence, recipients
- `describe.skip('schedules.delete')` — SCHED-03: deletes schedule by id
- `describe.skip('run-schedules cron')` — SCHED-02: finds due schedules (nextRunAt <= now AND enabled), generates report via generateEnrichedReport, advances nextRunAt
- `describe.skip('run-schedules email delivery')` — DIST-01: sends email after report generation when recipients configured; DIST-03: uses per-schedule recipients array
- `describe.skip('computeNextRun')` — unit tests for WEEKLY (+7d), MONTHLY (+1mo), QUARTERLY (+3mo), YEARLY (+1yr)
- `describe.skip('computeDateRange')` — unit tests for date range lookback per cadence

Use dynamic imports: `const { computeNextRun } = await import('@/lib/scheduling/schedule-helpers.js');`

**File 2: `__tests__/lib/adhoc-report/adhoc-chat.test.js`**
- `describe.skip('adhocReports.create')` — ADHC-01: creates AdHocReport record with status SCOPING
- `describe.skip('adhocReports.getChatMessages')` — ADHC-05: returns messages for a given adHocId ordered by createdAt
- `describe.skip('adhocReports.saveMessage')` — ADHC-05: persists user/assistant messages to AdHocReportMessage
- `describe.skip('adhocReports.generate')` — ADHC-01: transitions status SCOPING->GENERATING->READY, calls generateEnrichedReport with extracted params
- `describe.skip('adhocReports.rerun')` — ADHC-04: re-runs report generation with stored reportParams, creates new Report record
- `describe.skip('adhocReports.configureSnapshots')` — ADHC-03: sets snapshotIntervals and computes nextSnapshotAt
- `describe.skip('adhocReports.processSnapshots')` — ADHC-03: cron finds due snapshots (nextSnapshotAt <= now), regenerates report, advances nextSnapshotAt
- `describe.skip('ADHOC_SYSTEM_PROMPT')` — ADHC-02: prompt includes instructions to ask clarifying questions about time range, metrics focus, comparison baseline
- `describe.skip('extractReportParams')` — ADHC-02: extracts JSON params block from assistant message content

Each describe.skip block should have 1-2 `it` stubs with descriptive names matching the behavior being tested.
  </action>
  <verify>
    <automated>npx vitest run __tests__/lib/scheduling/ __tests__/lib/adhoc-report/ --reporter=verbose 2>&1 | grep -E "skipped|test files"</automated>
  </verify>
  <done>Both test files exist with describe.skip stubs covering all 11 requirement IDs. Running vitest shows them as skipped (not failing).</done>
</task>

</tasks>

<verification>
- `npx vitest run __tests__/lib/scheduling/ __tests__/lib/adhoc-report/ --reporter=verbose` shows all stubs as skipped
- No test failures in full suite: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- Two test stub files created covering SCHED-01..04, DIST-01, DIST-03, ADHC-01..05
- All stubs use describe.skip (no red tests)
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduling-ad-hoc-reports/07-00-SUMMARY.md`
</output>

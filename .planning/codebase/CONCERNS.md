# Concerns

> Auto-generated by GSD codebase mapper

## Security & Secrets

### Exposed Credentials in .env.local
- `.env.local` contains hardcoded live credentials including:
  - Vercel OIDC token (JWT)
  - PostgreSQL connection strings with passwords
  - SociaVault API key (`sk_live_*`)
  - Neon database credentials
- These should be moved to Vercel secrets and never committed to git
- No `.gitignore` entry found in codebase root to prevent accidental exposure

### Environment Variable Validation Gaps
- `lib/validate-env.js` validates `TOKEN_ENCRYPTION_KEY` format only in production mode
- Development deployments may silently use invalid encryption keys
- Missing validation for optional but security-critical variables like `REDDIT_CLIENT_ID`, `REDDIT_CLIENT_SECRET`

### Token Refresh Security Issues
- `lib/token-refresh.js` lacks error handling during token refresh failure
- No retry logic or exponential backoff for failed refreshes
- Decryption happens before validation (`decrypt(account.refreshToken)` on line 13)
- If decrypt fails, the error propagates up without wrapping

### Weak Cron Authentication
- `lib/cron-auth.js` uses simple string comparison (`authHeader === expected`)
- No timing attack protection
- Cron secret is a shared bearer token for all endpoints — no per-endpoint secrets
- Cron endpoints in `app/api/cron/*` all rely on this single authentication method

### Authentication Edge Case
- `lib/auth.js` line 48-56: Paradoxical logic where first user can be demoted from ADMIN to INTERNAL
- Auto-promotion logic may create security issues if user role changes between checks

## Error Handling & Reliability

### Missing Try-Catch Blocks
- `lib/x-adapter.js`: Network requests in class methods lack try-catch
  - `XOfficialClient.request()` line 26 throws on network failure with no handling
  - `TwitterAPIioClient.request()` line 141 throws on network failure with no handling
- `lib/reddit-adapter.js`: Same pattern as X adapter
  - `RedditAdapter.request()` line 22 throws on network failure with no handling

### Unhandled Promise Rejections
- `lib/ai.js` line 20-25: `generateInsight()` awaits Claude API but uses generic try-catch for `parseAIJSON()`
- Error handling swallows exceptions with bare `catch { ... }` blocks (lines 62-64, 70-74, 97-98)
- No logging or metrics for parse failures

### Missing Input Validation
- `lib/x-adapter.js` line 160: `searchTweets(query)` accepts raw query string without sanitization
- `lib/reddit-adapter.js` line 101: `searchSubreddit(subreddit, query)` doesn't validate subreddit name
- URLs constructed with user input via `encodeURIComponent()` but no length limits

### API Response Parsing Fragility
- `lib/x-adapter.js` line 190-193: Defensive field checking for author metadata
  - Uses hardcoded field names across multiple API versions
  - Falls back silently if fields missing (returns 0)
- `lib/sociavault.js` line 75: No validation of response structure before normalization

## Performance & Resource Management

### Cache without TTL Cleanup
- `lib/redis.js` defines `CACHE_TTL` constants but `cachedFetch()` line 26 sets expiry with `kv.set(..., { ex: ttlSeconds })`
- No eviction policy monitoring; Redis memory could grow unbounded if TTL increases
- `CacheEntry` table in schema may accumulate expired entries

### N+1 Query Patterns
- `app/api/cron/poll-metrics/route.js` line 58-75: Fetches all accounts, then all posts separately
  - Could combine queries or use batch operations
- Multiple sequential database calls in cron jobs (lines 79-108+) instead of batch operations

### Large Component File
- `components/SocialCommandCenter.jsx` is 62,852 tokens (mentioned in error)
- Contains massive mock datasets hardcoded in component
  - `accounts[]` array with hardcoded data (line 9-13)
  - `engagementData[]`, `followerData[]`, `postScatterData[]` (lines 15-39)
  - `listeningFeed[]` with 6 mock items (lines 72-79)
  - `kolActivations[]` with 5 items (lines 138-144)
- Should be split into smaller components and move mock data to separate fixtures

### Unbounded API Calls
- `lib/x-adapter.js` line 173: `getUserTimelinePaginated()` accepts `maxPages` parameter
- No internal rate limit check before making paginated requests
- Could exhaust X API rate limit quota in single call

## Data & Database Concerns

### Unencrypted Sensitive Data at Rest
- `lib/db.js` uses Prisma but doesn't specify encryption for sensitive columns
- OAuth tokens stored encrypted in `Account.accessToken` and `Account.refreshToken` (schema line 64-65)
- But no column-level encryption defined in Prisma schema

### JSON Storage Without Validation
- Schema line 479, 526, 594, 619, 651: Multiple `Json` columns without validation
  - `KOL.campaignDeliverables`, `KOL.aiScore*` fields
  - `KOLActivation.metricsAt*` fields
  - `AlertRule.conditions` and `AlertRule.actions`
  - No schema validation before storing
- Could accept malformed or oversized JSON

### Missing Foreign Key Constraints
- `schema.prisma` line 594-600: `AIInsight` has nullable `accountId` and `userId`
  - No check that at least one is provided
  - Could create orphaned insights
- `Post.threadId` (line 127) is nullable but `threadPosition` (line 128) is not — inconsistent
- `ListeningHit.accountId` (line 327) is nullable; hits could be global without assignment

### Audit Log Limitations
- `AuditLog` schema (line 674-687) logs user actions but missing critical context
  - No request correlation ID
  - No session ID tracking
  - `metadata` is unstructured JSON — difficult to query

## Configuration & Documentation

### Hardcoded API Endpoints
- `lib/x-adapter.js` line 20: X API base URL is hardcoded
- `lib/reddit-adapter.js` line 15: Reddit OAuth URL is hardcoded
- `lib/sociavault.js` line 15: SociaVault base URL is hardcoded
- No environment variable overrides for custom/testing endpoints

### Missing API Documentation
- No inline documentation for X Platform Adapter routing logic (line 2-8 comments are minimal)
- `lib/sociavault.js` mentions docs link but doesn't include request/response schema
- Cost estimates scattered across code (`$0.00015`, `$0.00024`, etc.) without central registry

### Inconsistent Environment Variable Naming
- Mix of prefixes: `X_`, `TWITTER`, `REDDIT_`, `ANTHROPIC_`, `KV_`, `POSTGRES_`
- Some redundant: `POSTGRES_PRISMA_URL`, `POSTGRES_URL_NON_POOLING`, `POSTGRES_URL`, `POSTGRES_URL_NO_SSL`
- No naming convention documented

### Missing Integration Tests
- `__tests__/lib/` directory exists but no test for adapters
- Only tests: `validate-env.test.js`, `encryption.test.js`, `cron-auth.test.js`
- No tests for API request logic, token refresh, or sentiment analysis

## Potential Bugs & Fragile Areas

### Division by Zero Risk
- `lib/ai/sentiment.js` line 67-68: `const positiveRatio = positiveCount / total`
  - `total` is initialized at line 61 but if both `positiveCount` and `negativeCount` are 0, `total = 0`
  - Falls back to neutral at line 63-64, but comment says confidence is undefined in edge case

### Type Coercion Issues
- `lib/reddit-adapter.js` line 36: `parseFloat(remaining)` for rate limit remaining
  - X adapter (line 39) uses `parseInt()` instead
  - Inconsistent parsing could cause comparison failures

### Date Handling Edge Cases
- `lib/x-adapter.js` line 28: `new Date(post.publishedAt).getTime()`
  - No null check; will throw if `publishedAt` is null
- Multiple places use `new Date()` without timezone consideration
- Schema uses `DateTime` but doesn't specify timezone handling

### Pagination Cursor Assumption
- `lib/x-adapter.js` line 199-203: Checks for `data?.next_cursor` to detect end of pagination
- TwitterAPI.io may use different field name or absence of field
- No defensive check for undefined cursor

### Missing Null Safety
- `lib/ai.js` line 44: `response.content?.[0]?.text || ''`
  - Safe access, but doesn't handle case where content is present but text is null
- `lib/x-adapter.js` line 75: `result.data.id` assumes `data` exists
  - publishThread() doesn't validate before accessing

## Cost & Quota Management

### Rough Cost Estimates
- `lib/x-adapter.js` line 50: Hardcoded POST estimate `0.01`, GET estimate `0.003`
  - No mention of actual X API v2 pricing
  - May not reflect real costs
- `lib/reddit-adapter.js` line 47: `0.00024` fixed cost
- `lib/sociavault.js` line 16: `0.0048` per credit without validation that cost is correct

### No Cost Alerts or Budgets
- `APICallLog` schema records costs but no alerts or budget enforcement
- No monthly cost cap or warnings before hitting limits
- Cron jobs may run without cost awareness

### Missing Rate Limit Monitoring
- Rate limit headers stored in Redis (line 35-40 in x-adapter, line 32-37 in reddit-adapter)
- But no pre-check before making requests; only logs after-the-fact
- Could cause burst of failed requests if approaching limits

## Code Duplication & Organization

### Duplicate Adapter Patterns
- `social-command-center/lib/` duplicates files from `lib/`:
  - `social-command-center/lib/db.js`
  - `social-command-center/lib/redis.js`
  - `social-command-center/lib/encryption.js`
  - `social-command-center/lib/x-adapter.js`
  - `social-command-center/lib/reddit-adapter.js`
- Creates maintenance burden; changes need to sync across both
- Unclear which version is canonical

### Monolithic Adapter Classes
- `XPlatformAdapter` in `lib/x-adapter.js` encapsulates two sub-adapters but methods delegate directly
- Could be harder to test or swap implementations
- No interface/contract defined

## Incomplete Features

### AI Score Storage
- Schema line 482: `aiScore` field is `String?` (A-F grades)
- No validation enum; allows any string
- `aiScoreUpdatedAt` (line 484) may not be set when score is updated

### KOL Metrics Gaps
- `KOLMetrics` line 549-568 includes estimated fields (`totalImpressionsEst`, `costPerEngagement`)
- But `activationsCount` is required while actual metrics (followers, engagement) may be estimates
- No way to distinguish estimated from measured data

### Listening Hit Status Tracking
- `ListeningHit` line 335: `routedToInboxItemId` links to inbox but no reciprocal relationship
- If inbox item is deleted, hit becomes orphaned
- No cascade delete defined

## Security Hardening Needed

1. Move all secrets from `.env.local` to Vercel environment
2. Add timing-safe token comparison in `cron-auth.js`
3. Add per-cron-job secrets instead of shared token
4. Implement retry logic with exponential backoff for token refresh
5. Add comprehensive error logging for all API calls
6. Validate all API responses before parsing
7. Add budget alerts for API costs
8. Separate and consolidate duplicate adapter files
9. Add integration tests for all adapters
10. Add rate limit pre-check before making requests

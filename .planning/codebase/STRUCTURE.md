# STRUCTURE

> Auto-generated by GSD codebase mapper

## Directory Layout

```
/Users/michelleso/Social Command/
├── social-command-center/              # Main application (Next.js 14)
│   ├── app/                            # Next.js App Router
│   │   ├── layout.jsx                  # Root layout
│   │   ├── page.jsx                    # Entry page (/ route)
│   │   ├── globals.css                 # Tailwind base styles
│   │   └── (dashboard)/                # Grouped dashboard routes
│   │       ├── layout.jsx              # Dashboard shell + nav
│   │       ├── page.jsx                # Dashboard home (/dashboard)
│   │       ├── composer/
│   │       │   └── page.jsx            # Post composer
│   │       ├── calendar/
│   │       │   └── page.jsx            # Content calendar
│   │       ├── listening/
│   │       │   └── page.jsx            # Social listening
│   │       ├── kol/
│   │       │   └── page.jsx            # KOL tracking
│   │       ├── reports/
│   │       │   └── page.jsx            # Reporting
│   │       └── admin/
│   │           └── page.jsx            # Admin panel
│   │
│   ├── components/                     # React components
│   │   ├── SocialCommandCenter.jsx     # Root client component
│   │   └── ui.jsx                      # Shared UI utilities
│   │
│   ├── lib/                            # Business logic & utilities
│   │   ├── db.js                       # Prisma client singleton
│   │   ├── redis.js                    # Vercel KV client + cache TTLs
│   │   ├── encryption.js               # AES-256-GCM token encryption
│   │   ├── x-adapter.js                # X/Twitter API adapter (official + 3rd-party)
│   │   ├── reddit-adapter.js           # Reddit API adapter
│   │   └── trpc-client.js              # tRPC React client setup
│   │
│   ├── prisma/
│   │   └── schema.prisma               # Prisma schema (PostgreSQL)
│   │
│   ├── public/                         # Static assets
│   │   ├── file.svg
│   │   ├── next.svg
│   │   ├── globe.svg
│   │   ├── window.svg
│   │   └── vercel.svg
│   │
│   ├── package.json                    # Dependencies & scripts
│   ├── next.config.js                  # Next.js config (standalone output)
│   ├── tailwind.config.js              # Tailwind CSS config
│   ├── postcss.config.js               # PostCSS config
│   ├── .env.example                    # Environment variables template
│   ├── .gitignore                      # Git ignore rules
│   └── CLAUDE-CODE-BUILD-GUIDE.md      # Build documentation
│
├── node_modules/                       # Dependencies
├── .git/                               # Git repository
├── .planning/                          # Planning & documentation
│   └── codebase/
│       ├── ARCHITECTURE.md             # This file
│       └── STRUCTURE.md                # Directory & file organization
│
└── public/                             # (Root-level) Static assets fallback
    └── [static files]
```

## Key File Locations

### Frontend Pages

**Dashboard & Main UI:**
| File | Purpose |
|------|---------|
| `social-command-center/app/page.jsx` | Root page, renders `SocialCommandCenter` component |
| `social-command-center/app/(dashboard)/page.jsx` | Dashboard home (analytics overview) |
| `social-command-center/app/(dashboard)/layout.jsx` | Dashboard wrapper with tab navigation |
| `social-command-center/app/(dashboard)/composer/page.jsx` | Post composer & scheduler |
| `social-command-center/app/(dashboard)/calendar/page.jsx` | Content calendar view |
| `social-command-center/app/(dashboard)/listening/page.jsx` | Social listening feed + AI insights |
| `social-command-center/app/(dashboard)/kol/page.jsx` | KOL tracking dashboard |
| `social-command-center/app/(dashboard)/reports/page.jsx` | Reports & exports |
| `social-command-center/app/(dashboard)/admin/page.jsx` | Admin settings |

### Components

| File | Purpose |
|------|---------|
| `social-command-center/components/SocialCommandCenter.jsx` | Root client component (proto, may be deprecated) |
| `social-command-center/components/ui.jsx` | Shared UI: PlatformBadge, MetricCard, Avatar, Skeleton, charts |

### Libraries & Utilities

| File | Purpose |
|------|---------|
| `social-command-center/lib/db.js` | Prisma client singleton |
| `social-command-center/lib/redis.js` | Vercel KV Redis client + cache TTL constants |
| `social-command-center/lib/encryption.js` | AES-256-GCM encryption/decryption for OAuth tokens |
| `social-command-center/lib/x-adapter.js` | X API adapter (official + TwitterAPI.io routing) |
| `social-command-center/lib/reddit-adapter.js` | Reddit API adapter |
| `social-command-center/lib/trpc-client.js` | tRPC React client |

### Configuration

| File | Purpose |
|------|---------|
| `social-command-center/package.json` | npm scripts: dev, build, lint, db:* |
| `social-command-center/next.config.js` | Next.js config (standalone output) |
| `social-command-center/tailwind.config.js` | Tailwind CSS configuration |
| `social-command-center/postcss.config.js` | PostCSS plugins |
| `social-command-center/prisma/schema.prisma` | Database schema definition |
| `social-command-center/.env.example` | Example environment variables |

### Documentation

| File | Purpose |
|------|---------|
| `social-command-center/CLAUDE-CODE-BUILD-GUIDE.md` | Build & deployment guide |
| `.planning/codebase/ARCHITECTURE.md` | Architecture overview |
| `.planning/codebase/STRUCTURE.md` | This file |

## Naming Conventions

### Files

**React Components:**
- PascalCase: `SocialCommandCenter.jsx`, `MetricCard.jsx`
- Stored in `components/` directory

**Utility Functions:**
- camelCase: `x-adapter.js`, `redis.js`, `encryption.js`
- Stored in `lib/` directory

**Configuration Files:**
- Lowercase with dot: `next.config.js`, `tailwind.config.js`, `postcss.config.js`
- At root of project

**Pages (App Router):**
- `page.jsx` for route files
- `layout.jsx` for route layouts
- Named route directories: `(dashboard)`, `listening`, `kol`, etc.

### Directories

**By Feature/Layer:**
- `app/` — Next.js App Router pages and layouts
- `components/` — Reusable React components
- `lib/` — Business logic, adapters, utilities
- `prisma/` — Database schema and migrations
- `public/` — Static assets

**Grouping Convention (Route Groups):**
- `(dashboard)/` — Grouped routes, doesn't add to URL path
- All dashboard sub-pages inherit the dashboard layout

### Variables & Constants

**Colors (in `components/ui.jsx`):**
```javascript
export const COLORS = {
  blue: "#3b82f6",
  green: "#22c55e",
  red: "#ef4444",
  // ... etc
};
```

**Cache TTLs (in `lib/redis.js`):**
```javascript
export const CACHE_TTL = {
  USER_PROFILE: 86400,        // seconds
  MENTIONS: 300,
  SEARCH_RESULTS: 1800,
  // ... etc
};
```

**tRPC Procedure Paths:**
- Dot notation: `trpc.analytics.dashboard`, `trpc.listening.hits.list`

## File Organization Patterns

### Component Files (in `components/`)

Single file per simple component or one file for a related group:

```
components/
├── SocialCommandCenter.jsx   # Root/demo component
└── ui.jsx                    # All shared UI utilities & badges
```

### Page Files (in `app/`)

Each route has a `page.jsx` for rendering and `layout.jsx` for wrapper:

```
app/
├── layout.jsx                         # Root layout
├── page.jsx                           # Root page
├── (dashboard)/
│   ├── layout.jsx                     # Dashboard layout + nav
│   ├── page.jsx                       # Dashboard home
│   ├── listening/page.jsx             # Listening page
│   └── kol/page.jsx                   # KOL page
```

### Library Files (in `lib/`)

Each integration/utility gets its own module:

```
lib/
├── db.js                   # Prisma singleton
├── redis.js                # Vercel KV + caching
├── encryption.js           # Token encryption
├── x-adapter.js            # X API integration
├── reddit-adapter.js       # Reddit API integration
└── trpc-client.js          # tRPC client setup
```

### Database (in `prisma/`)

```
prisma/
└── schema.prisma           # Prisma schema (all models in one file)
```

## Import Paths

The project uses path aliases (configured in Next.js) for cleaner imports:

```javascript
// Instead of:
import { trpc } from '../../../lib/trpc-client';

// Use:
import { trpc } from '@/lib/trpc-client';
```

**Common Alias Locations:**
- `@/app/` — App directory
- `@/components/` — Components directory
- `@/lib/` — Library directory
- `@/prisma/` — Prisma directory

## Code Organization Best Practices (Inferred)

### 1. Client vs Server

**Client Components (`'use client'`):**
- Dashboard pages: all use `'use client'` for interactive state
- Form components
- Real-time chart updates

**Server Components (implicit):**
- Layout wrappers (middleware-like)
- Could leverage server-side rendering for initial loads

### 2. Data Flow

**Queries (Read):**
```
Page component
  → trpc.{namespace}.{procedure}.useQuery()
  → tRPC server procedure
  → Prisma queries + adapter calls
  → Cache layer (Redis)
  → Platform APIs (X, Reddit)
```

**Mutations (Write):**
```
Form component
  → trpc.{namespace}.{procedure}.useMutation()
  → tRPC server procedure
  → Validate input (Zod)
  → Encrypt tokens if needed
  → Call platform adapter
  → Update database
  → Return result
```

### 3. State Management

**UI State:**
- React hooks (`useState`, `useMemo`)
- Zustand 4.5 for shared state (if used)

**Data State:**
- React Query via tRPC (automatic caching)
- Manual cache invalidation on mutations

### 4. Error Handling

**Not shown in detail, but pattern would be:**
- Try/catch in tRPC procedures
- Fallback UI states (skeletons, empty states)
- User-facing error messages

## Build & Deployment

### Scripts (from `package.json`)

```bash
npm run dev              # Local dev: next dev
npm run build            # Production build: next build
npm run start            # Run production: next start
npm run lint             # ESLint check: next lint
npm run db:generate      # Prisma: generate client
npm run db:push          # Prisma: push schema to DB
npm run db:migrate       # Prisma: create migration & apply
npm run db:studio        # Prisma: open Studio UI
npm run db:seed          # Prisma: run seed script
```

### Build Configuration

**Output:** `next.config.js` sets `output: 'standalone'`
- Produces a self-contained Node.js server
- Can be deployed to Vercel, Docker, or any Node host

### Environment Variables

See `social-command-center/.env.example` for required vars:
- `POSTGRES_PRISMA_URL` — Database connection
- `KV_REST_API_URL`, `KV_REST_API_TOKEN` — Vercel KV
- `X_*` (Official API keys + TwitterAPI.io keys)
- `REDDIT_*` (OAuth credentials)
- `CLAUDE_API_KEY` — Anthropic SDK
- `TOKEN_ENCRYPTION_KEY` — AES key (hex)
- `NEXTAUTH_SECRET` — Session secret

## File Summary

| Category | Count | Key Files |
|----------|-------|-----------|
| Pages | 9 | page.jsx files (app/ + (dashboard)/* subdirs) |
| Components | 2 | SocialCommandCenter.jsx, ui.jsx |
| Libraries | 6 | db, redis, encryption, x-adapter, reddit-adapter, trpc-client |
| Config | 6 | next.config.js, tailwind.config.js, postcss.config.js, prisma/schema.prisma, package.json, .env.example |
| Styles | 1 | app/globals.css |
| Assets | 5+ | public/ static SVGs |

**Total Source Files:** ~30 files (excluding node_modules, .git)

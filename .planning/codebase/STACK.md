# Technology Stack

> Auto-generated by GSD codebase mapper

## Runtime & Framework

- **Node.js Runtime**: Next.js 14.2 (`next@^14.2.0`)
- **Frontend Framework**: React 18.3 (`react@^18.3.0`, `react-dom@^18.3.0`)
- **Backend Framework**: Next.js with App Router (`next@^14.2.0` with `output: standalone`)
- **Type System**: TypeScript (configuration via `.planning/tsconfig.json`)

## Build Tools

- **Bundler**: Next.js built-in bundler
- **CSS Processing**: Tailwind CSS 3.4 (`tailwindcss@^3.4.0`) with PostCSS (`postcss@^8.4.0`)
- **CSS Vendor Prefixing**: Autoprefixer (`autoprefixer@^10.4.0`)

See configuration files:
- `next.config.js` (root, empty base config)
- `social-command-center/next.config.js` (standalone output config)
- `tailwind.config.js`
- `postcss.config.js`

## ORM & Database

- **ORM**: Prisma 5.14 (`@prisma/client@^5.14.0`, `prisma@^5.14.0`)
- **Database**: PostgreSQL (Vercel Postgres) via Prisma
- **Database Schema**: `prisma/schema.prisma` (comprehensive multi-feature schema)
- **Database Client**: `lib/db.js` (Prisma singleton with conditional logging)

## API & Backend

- **RPC Framework**: tRPC 10.45 (`@trpc/server@^10.45.0`, `@trpc/next@^10.45.0`, `@trpc/client@^10.45.0`, `@trpc/react-query@^10.45.0`)
- **HTTP Client**: Fetch API (native)
- **JSON Parsing**: SuperJSON (`superjson@^2.2.6`)

## State Management & Data Fetching

- **Server State**: React Query 4.36 (`@tanstack/react-query@^4.36.0`)
- **Client State**: Zustand 4.5 (`zustand@^4.5.0`)

## Authentication

- **Auth Framework**: NextAuth.js 4.24 (`next-auth@^4.24.0`)
- **Auth Method**: Credentials provider with team access code
- **OAuth Providers**: Google OAuth (configured, not yet in code)
- **Auth Configuration**: `lib/auth.js`

## Caching & Session Storage

- **Cache/Session Backend**: Vercel KV (Redis) (`@vercel/kv@^2.0.0`)
- **Redis Client**: `lib/redis.js` (KV wrapper with TTL cache strategy)

## File Storage

- **Blob Storage**: Vercel Blob (`@vercel/blob@^0.23.0`)

## Validation & Serialization

- **Schema Validation**: Zod 3.23 (`zod@^3.23.0`)
- **JSON Serialization**: SuperJSON 2.2 (`superjson@^2.2.6`)

## AI & LLM

- **AI SDK**: Anthropic SDK 0.24 (`@anthropic-ai/sdk@^0.24.0`)
- **Default Model**: Claude 3.5 Haiku (`claude-3-5-haiku-20241022`)
- **AI Integration Files**:
  - `lib/ai.js` (generic insight generator with cost tracking)
  - `lib/ai/sentiment.js` (sentiment analysis)
  - `lib/ai/reports.js` (report generation)
  - `lib/ai/content-suggestions.js` (content AI)
  - `lib/ai/kol-scoring.js` (KOL scoring with AI)

## Data Visualization

- **Charts Library**: Recharts 2.12 (`recharts@^2.12.0`)

## Email

- **Email Client**: Nodemailer 7.0 (`nodemailer@^7.0.13`)

## Testing

- **Test Framework**: Vitest 4.0.18 (`vitest@^4.0.18`)
- **Coverage Provider**: V8 (`@vitest/coverage-v8@^4.0.18`)
- **Test Config**: `vitest.config.js`
- **Test Directory**: `__tests__/` with setup file

## Linting & Code Quality

- **Linter**: ESLint 8.0 (`eslint@^8.0.0`)
- **ESLint Config**: Next.js preset (`eslint-config-next@^14.2.0`)

## Server-Side Integration Files

**Social Media Adapters**:
- `lib/x-adapter.js` (X/Twitter with hybrid read/write routing)
- `lib/reddit-adapter.js` (Reddit OAuth 2.0 official API)
- `lib/adapters/index.js` (adapter exports)

**Third-Party Integrations**:
- `lib/sociavault.js` (SociaVault API for Reddit scraping)
- `lib/mention-classifier.js` (mention classification)
- `lib/listening-scanner.js` (social listening polling)

**Business Logic Modules**:
- `lib/routers/` (tRPC routers for accounts, posts, inbox, listening, competitors, kol, analytics, reports, admin, ai, app)
- `lib/token-refresh.js` (OAuth token refresh)
- `lib/encryption.js` (AES-256-GCM token encryption)
- `lib/account-context.js` (account context management)
- `lib/trpc.js` (tRPC configuration)
- `lib/trpc-client.js` (tRPC client setup)
- `lib/cron-auth.js` (cron job authentication)
- `lib/cron-monitor.js` (cron monitoring)
- `lib/validate-env.js` (environment variable validation)

## Environment Configuration

- Root `.env.example` (comprehensive env vars for all integrations)
- `social-command-center/.env.example` (app-level env vars)
- Key variables tracked in `lib/validate-env.js`

## API Routes (Next.js App Router)

**Authentication**:
- `app/api/auth/[...nextauth]/route.js` (NextAuth handler)
- `app/auth/signin/page.jsx` (sign-in page)
- `app/auth/verify/page.jsx` (verification page)

**Platform Connections**:
- `app/api/connect/x/route.js` (X OAuth init)
- `app/api/connect/x/callback/route.js` (X OAuth callback)
- `app/api/connect/reddit/route.js` (Reddit OAuth init)
- `app/api/connect/reddit/callback/route.js` (Reddit OAuth callback)

**tRPC**:
- `app/api/trpc/[trpc]/route.js` (tRPC endpoint)

**Cron Jobs**:
- `app/api/cron/publish-scheduled/route.js` (post publishing, 1m interval)
- `app/api/cron/poll-listening/route.js` (social listening polling)
- `app/api/cron/poll-mentions/route.js` (mention detection)
- `app/api/cron/poll-metrics/route.js` (post metrics refresh)
- `app/api/cron/poll-competitors/route.js` (competitor tracking)
- `app/api/cron/poll-subreddit-metrics/route.js` (subreddit metrics)
- `app/api/cron/daily-analytics/route.js` (daily aggregation)
- `app/api/cron/weekly-ai-insights/route.js` (AI insight generation)
- `app/api/cron/aggregate-kol-metrics/route.js` (KOL metrics aggregation)
- `app/api/cron/backfill-history/route.js` (data backfilling)

## UI Pages (Next.js App Router)

**Dashboard Layout**:
- `app/layout.jsx` (root layout)
- `app/(dashboard)/layout.jsx` (dashboard layout)

**Dashboard Pages**:
- `app/(dashboard)/page.jsx` (homepage)
- `app/(dashboard)/composer/page.jsx` (post composer)
- `app/(dashboard)/calendar/page.jsx` (scheduling calendar)
- `app/(dashboard)/listening/page.jsx` (social listening)
- `app/(dashboard)/kol/page.jsx` (KOL tracking)
- `app/(dashboard)/reports/page.jsx` (reports generation)
- `app/(dashboard)/admin/page.jsx` (admin panel)

**Styling**:
- `app/globals.css` (global stylesheet)

## Database Schema (Prisma)

Key models in `prisma/schema.prisma`:
- **Auth**: User, Session, VerificationToken
- **Social Accounts**: Account, UserAccountAccess
- **Posts**: Post, PostMetrics, AccountMetrics
- **Mentions & Inbox**: Mention, InboxItem
- **Social Listening**: ListeningTopic, ListeningQuery, ListeningHit, MonitoredSubreddit, SubredditMetrics
- **Competitors**: Competitor, CompetitorAccount, CompetitorKeyword, CompetitorMetrics
- **KOLs**: KOL, KOLCohort, KOLActivation, KOLMetrics
- **AI & Alerts**: AlertRule, AIInsight
- **Workflows**: ApprovalRequest
- **Reports**: Report
- **Audit**: AuditLog
- **Cost Tracking**: APICallLog
- **Caching**: CacheEntry

## Package Management

- **Package Manager**: npm (via `package.json`)
- **Root Package**: `package.json` (main app)
- **App Subpackage**: `social-command-center/package.json` (web app, deprecated structure)

## Deployment

- **Platform**: Vercel (indicated by Vercel-specific libraries: KV, Blob, Postgres)
- **Standalone Output**: Configured in `social-command-center/next.config.js`

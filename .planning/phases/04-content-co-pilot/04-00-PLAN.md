---
phase: 04-content-co-pilot
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/lib/copilot/intel-context.test.js
  - __tests__/lib/copilot/brand-voice.test.js
  - __tests__/lib/copilot/draft-detector.test.js
  - __tests__/lib/copilot/prediction.test.js
autonomous: true
requirements:
  - CPLT-02
  - CPLT-03
  - CPLT-04
  - CPLT-05

must_haves:
  truths:
    - "Test stubs exist for all copilot library modules before implementation begins"
    - "Tests use dynamic imports so they compile before source modules exist"
  artifacts:
    - path: "__tests__/lib/copilot/intel-context.test.js"
      provides: "Test stub for intel context condensation"
    - path: "__tests__/lib/copilot/brand-voice.test.js"
      provides: "Test stub for brand voice fetching"
    - path: "__tests__/lib/copilot/draft-detector.test.js"
      provides: "Test stub for draft content detection"
    - path: "__tests__/lib/copilot/prediction.test.js"
      provides: "Test stub for performance prediction"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for copilot library modules.

Purpose: Establish test stubs with dynamic imports before source modules exist, following the project's Wave 0 pattern from Phase 06.
Output: 4 test files with skipped stubs covering intel context, brand voice, draft detection, and prediction.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-co-pilot/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for copilot lib modules</name>
  <files>
    __tests__/lib/copilot/intel-context.test.js,
    __tests__/lib/copilot/brand-voice.test.js,
    __tests__/lib/copilot/draft-detector.test.js,
    __tests__/lib/copilot/prediction.test.js
  </files>
  <action>
Create 4 test files using the project's Wave 0 pattern (dynamic imports, describe.skip blocks). Follow the Phase 06 pattern from `__tests__/lib/listening-scanner.test.js` for structure.

**__tests__/lib/copilot/intel-context.test.js** (CPLT-02):
- `describe.skip('lib/copilot/intel-context')` with dynamic import of `@/lib/copilot/intel-context`
- Test: "getCondensedIntelSummary returns combined intel from 3 sources" — expects string containing performance, competitor, and audience sections
- Test: "returns fallback message when no intel data exists" — expects fallback string about running cron
- Test: "respects ~500 token budget" — expects output length under 2500 chars (~500 tokens)

**__tests__/lib/copilot/brand-voice.test.js** (CPLT-03):
- `describe.skip('lib/copilot/brand-voice')` with dynamic import of `@/lib/copilot/brand-voice`
- Test: "getTopPostsForAccount returns top posts sorted by engagement" — expects array of 3-5 posts with content, contentType, engagementRate fields
- Test: "returns empty array when accountId is null" — expects []
- Test: "truncates post content to 200 chars" — expects no content longer than 200

**__tests__/lib/copilot/draft-detector.test.js** (CPLT-05):
- `describe.skip('lib/copilot/draft-detector')` with dynamic import of `@/lib/copilot/draft-detector`
- Test: "detects explicit draft markers" — input with "Here's a draft:" returns true
- Test: "detects thread format" — input with "1. First tweet\n2. Second tweet" returns true
- Test: "returns false for conversational messages" — input "What topics should I write about?" returns false

**__tests__/lib/copilot/prediction.test.js** (CPLT-04):
- `describe.skip('lib/copilot/prediction')` with dynamic import of `@/lib/copilot/prediction`
- Test: "formatPredictionCard returns score card object" — expects { engagementRate, impressionsEstimate, confidence, comparisonToAvg }
- Test: "handles missing account context gracefully" — expects result with null comparisonToAvg

Mock `@/lib/db` (prisma) in all test files that need it. Use `vi.mock` with factory function.
  </action>
  <verify>
    <automated>cd "/Users/michelleso/Social Command" && npx vitest run __tests__/lib/copilot/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 test files exist in __tests__/lib/copilot/, all pass (skipped stubs), vitest suite stays green</done>
</task>

</tasks>

<verification>
- `npx vitest run __tests__/lib/copilot/ --reporter=verbose` shows all skipped tests
- Full suite `npx vitest run --reporter=verbose` still passes
</verification>

<success_criteria>
Wave 0 test stubs exist for intel-context, brand-voice, draft-detector, and prediction modules. All use dynamic imports and describe.skip. Full test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-co-pilot/04-00-SUMMARY.md`
</output>

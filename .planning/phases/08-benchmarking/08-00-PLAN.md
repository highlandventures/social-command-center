---
phase: 08-benchmarking
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/lib/milestones.test.js
  - __tests__/lib/benchmark-compare.test.js
autonomous: true
requirements:
  - BNCH-01
  - BNCH-02
  - BNCH-03

must_haves:
  truths:
    - "Test stubs exist for milestone CRUD and benchmark comparison logic"
    - "Test files compile and are skipped (not failing) in full suite"
  artifacts:
    - path: "__tests__/lib/milestones.test.js"
      provides: "Milestone CRUD router test stubs"
      contains: "describe.skip"
    - path: "__tests__/lib/benchmark-compare.test.js"
      provides: "Benchmark comparison logic test stubs"
      contains: "describe.skip"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for Phase 8 benchmarking -- milestone CRUD and benchmark comparison logic.

Purpose: Establish test contracts before implementation so Plan 01 can unskip and fill in tests as code lands.
Output: Two test files with describe.skip blocks covering BNCH-01, BNCH-02, BNCH-03.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-benchmarking/08-RESEARCH.md
@.planning/phases/08-benchmarking/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for milestones and benchmark comparison</name>
  <files>__tests__/lib/milestones.test.js, __tests__/lib/benchmark-compare.test.js</files>
  <action>
Create two test files following the project's Wave 0 pattern (describe.skip with dynamic imports, prisma mocked at top).

**__tests__/lib/milestones.test.js** — Stubs for BNCH-02 (milestone CRUD):
- Mock `@/lib/db` with prisma.milestone.findMany, create, update, delete
- `describe.skip('milestonesRouter')`:
  - `it('list: returns all milestones ordered by startDate desc')`
  - `it('create: creates milestone with name, description, startDate, endDate')`
  - `it('create: rejects endDate before startDate')`
  - `it('update: updates milestone fields')`
  - `it('delete: removes milestone by id')`
- Use dynamic `await import('@/lib/routers/milestones.js')` inside describe block

**__tests__/lib/benchmark-compare.test.js** — Stubs for BNCH-01, BNCH-03:
- Mock `@/lib/db` with prisma.post, prisma.accountMetrics, prisma.listeningHit, prisma.milestone
- `describe.skip('computeBenchmarkDeltas')`:
  - `it('computes KPI deltas between two periods')`
  - `it('returns flat direction when previous period has no data')`
  - `it('handles null/zero previous values gracefully')`
- `describe.skip('resolveComparisonPeriod')`:
  - `it('resolves WoW to previous week date range')`
  - `it('resolves MoM to previous month date range')`
  - `it('resolves QoQ to previous quarter date range')`
  - `it('resolves YoY to previous year date range')`
- `describe.skip('benchmark against milestone')`:
  - `it('resolves milestone ID to date range and computes deltas')`
  - `it('returns error message when milestone period has no data')`
- Use dynamic imports for `@/lib/benchmark-compare.js`

Follow existing patterns from `__tests__/lib/report-engine.test.js` and Phase 7 Wave 0 stubs.
  </action>
  <verify>
    <automated>cd "/Users/michelleso/Social Command" && npx vitest run __tests__/lib/milestones.test.js __tests__/lib/benchmark-compare.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>Both test files exist with describe.skip blocks. Full test suite passes (skipped tests do not fail). Test contracts cover BNCH-01 (period comparison), BNCH-02 (milestone CRUD), BNCH-03 (milestone benchmark).</done>
</task>

</tasks>

<verification>
- `npx vitest run __tests__/lib/milestones.test.js __tests__/lib/benchmark-compare.test.js` completes with skipped tests
- `npx vitest run` full suite still green (no regressions)
</verification>

<success_criteria>
- Two test stub files exist and compile
- All describe blocks are skipped (not failing)
- Full suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/08-benchmarking/08-00-SUMMARY.md`
</output>
